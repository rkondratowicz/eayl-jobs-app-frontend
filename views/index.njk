<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            max-width: 800px;
            margin: 0 auto;
            padding: 2rem;
            background-color: #f8fafc;
            color: #334155;
        }
        .header {
            text-align: center;
            margin-bottom: 2rem;
        }
        .header h1 {
            color: #0f172a;
            font-size: 2.5rem;
            margin-bottom: 0.5rem;
        }
        .header p {
            color: #64748b;
            font-size: 1.1rem;
        }
        .card {
            background: white;
            border-radius: 0.5rem;
            box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1);
            padding: 1.5rem;
            margin-bottom: 1rem;
        }
        .highlight {
            background-color: #dbeafe;
            padding: 1rem;
            border-radius: 0.375rem;
            border-left: 4px solid #3b82f6;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>{{ title }}</h1>
        <p>{{ description }}</p>
    </div>

    <div class="card">
        <div class="highlight">
            <h2>Welcome to your Nunjucks-powered app!</h2>
            <p>This template is rendered using Nunjucks templating engine.</p>
            <p><strong>Message:</strong> {{ message }}</p>
        </div>
    </div>

    <div class="card">
        <h3>Template Features</h3>
        <ul>
            <li>Server-side rendering with Nunjucks</li>
            <li>TypeScript support</li>
            <li>Express.js backend</li>
            <li>Modern CSS styling</li>
        </ul>
    </div>
</body>
</html>