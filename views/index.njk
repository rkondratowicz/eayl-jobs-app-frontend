<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body class="bg-slate-50 text-slate-700 max-w-4xl mx-auto p-8">
    <header class="text-center mb-8">
        <h1 class="text-slate-900 text-4xl font-bold mb-2">{{ title }}</h1>
        <p class="text-slate-600 text-lg">{{ description }}</p>
    </header>

    <div class="bg-white rounded-lg shadow-sm p-6 mb-4">
        <div class="bg-blue-50 p-4 rounded-md border-l-4 border-blue-500">
            <h2 class="text-xl font-semibold mb-2 text-gray-900">Welcome to your Nunjucks-powered app!</h2>
            <p class="mb-2 text-gray-700">This template is now styled with Tailwind CSS and rendered using Nunjucks templating engine.</p>
            <p class="text-gray-700"><strong class="font-medium">Message:</strong> {{ message }}</p>
        </div>
    </div>

    <div class="bg-white rounded-lg shadow-sm p-6">
        <h3 class="text-lg font-semibold mb-4 text-gray-900">Template Features</h3>
        <ul class="space-y-2 text-gray-700">
            <li class="flex items-start">
                <span class="inline-block w-2 h-2 bg-blue-500 rounded-full mt-2 mr-3 flex-shrink-0"></span>
                Server-side rendering with Nunjucks
            </li>
            <li class="flex items-start">
                <span class="inline-block w-2 h-2 bg-blue-500 rounded-full mt-2 mr-3 flex-shrink-0"></span>
                TypeScript support
            </li>
            <li class="flex items-start">
                <span class="inline-block w-2 h-2 bg-blue-500 rounded-full mt-2 mr-3 flex-shrink-0"></span>
                Express.js backend
            </li>
            <li class="flex items-start">
                <span class="inline-block w-2 h-2 bg-blue-500 rounded-full mt-2 mr-3 flex-shrink-0"></span>
                Tailwind CSS styling with CLI setup
            </li>
        </ul>
    </div>
</body>
</html>